# Leave blank in local development: photos are read from the ignored /media folder.
# In production, point to a CDN or object-storage custom domain. Do not add a trailing slash.
NEXT_PUBLIC_MEDIA_BASE_URL=https://images.example.com
